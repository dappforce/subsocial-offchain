# docker-compose.yml
version: "3"
services:
  postgres:
    image: postgres
    environment:
      POSTGRES_DB: subsocial
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: 1986
    ports:
      - "5432:5432"
    restart: always
    volumes:
      - ~/dappforce/postgres-data:/var/lib/postgresql/data
      
  subsocial-offchain:
    environment:
      PGHOST: postgres
    build:
      context: ..
      dockerfile: ./docker/Dockerfile
    image: dappforce/subsocial-offchain:latest
    depends_on:
      - postgres
    ports:
      - "3001:3001"
    command: bash -c "yarn init-offchain && yarn start"

import { newLogger } from '@subsocial/utils';

export const postgesLog = newLogger('postgres');
export const searchLog = newLogger('elastic-search');
export const substrateLog = newLogger('substrate');

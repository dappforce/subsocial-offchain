<!DOCTYPE html>
<html>

<head>
  <title>Notifications</title>
  <meta charset="utf-8">
  <style type="text/css">
    .background {
      background: #fafafa;
      padding-bottom: 1rem;
    }

    .segment {
      background-color: #fff;
      padding: 1rem;
      position: relative;
      margin: 0 0 1rem 25%;
      border-radius: 4px;
      border: 1px solid #ddd;
      width: 44.5rem;
      margin-bottom: 1rem;
    }

    .title {
      margin: 0 0 0 25%;
      font-size: 1.5rem;
    }

    .feed-item {
      display: block;
      justify-content: space-between;
    }

    .feed-row {
      display: flex;
      width: 100%;
      color: #222222;
      min-height: 60px;
    }

    hr {
      width: 46.5rem;
      margin: 0 0 0.75rem 25%
    }

    .row-content {
      margin-left: 40px;
      margin: 0 .75rem 0 1rem;
      width: inherit;
    }

    a {
      font-weight: bold;
      font-weight: bold;
      text-decoration: none;
      display: inline-block;
      cursor: pointer;
      color: #222222 !important;

    }

    a:hover {
      cursor: pointer;
    }

    .image {
      width: 36px;
      height: 36px;
      min-width: 36px;
      min-height: 36px;
      border-radius: 50%;
    }

    .text-activity {
      display: inline-flex;
    }

    .activity-item {
      margin-left: 0.35rem;
    }

    .space {
      font-size: 0.875rem;
      color: #888 !important;
      font-weight: normal;
    }

    .post-body {
      width: 44.4rem;
      font-size: 1rem;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol', 'Noto Color Emoji';
    }

    .post {
      font-size: 1.5rem;
      color: #222222 !important;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol', 'Noto Color Emoji';
    }

    .date-and-space {
      width: 100%;
      font-size: 0.875rem;
      color: #888;
    }
  </style>
</head>

<body>
  <div class='background'>
    <p class="title">My feed</p>
    <hr>
    {{#each data}}
    <div class="segment">
      <div class='feed-item'>
        <div class="feed-row">
          {{#notEq avatar ''}}
          <img src="{{avatar}}" class='image' />
          {{/notEq}}
          <div class="row-content">
            <div class='text-activity'>
              <a href="{{ownerLink}}">{{ownerName}}</a>
            </div>

            <div class='date-and-space'>
              <a class="space" href="{{spaceLink}}">{{spaceName}}</a> • {{date}}
            </div>

          </div>
        </div>
        <div class="feed-content">
          <div>
            <a class="post" href="{{postLink}}">{{postName}}</a>
          </div>
          <div>
            <p class="post-body">{{postBody}}</p>
          </div>
        </div>
      </div>
    </div>
    {{/each}}
  </div>
</body>
<html>
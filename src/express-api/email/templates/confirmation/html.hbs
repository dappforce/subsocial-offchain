<!DOCTYPE html>
<html>

<head>
  <title>Notifications</title>
  <meta charset="utf-8">
  <style type="text/css">
    .background {
      padding-bottom: 1rem;
      padding-top: 1rem;
    }

    .image {
      margin-top: 10px;
      width: 30px;
    }

    .buttonLink {
      color: rgb(255, 255, 255) !important;
      text-decoration: none;
      display: inline-block;
      cursor: pointer;
    }

    button {
      color: #fff;
      background: #eb2f96;
      border-color: #eb2f96;
      text-shadow: 0 -1px 0 rgba(0, 0, 0, 0.12);
      box-shadow: 0 2px 0 rgba(0, 0, 0, 0.045);
      line-height: 1.5715;
      position: relative;
      display: inline-block;
      font-weight: 400;
      white-space: nowrap;
      text-align: center;
      border: 1px solid transparent;
      box-shadow: 0 2px 0 rgba(0, 0, 0, 0.015);
      cursor: pointer;
      touch-action: manipulation;
      height: 32px;
      padding: 4px 15px;
      font-size: 14px;
      border-radius: 2px;
      color: rgba(0, 0, 0, 0.85);
      margin-bottom: 10px;
    }

    p {
      font-size: 1rem;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol', 'Noto Color Emoji';
    }

    .confirmation-item {
      text-align: center;
      width: 100%;
      color: #222222;
      min-height: 60px;
    }

    .confirmation-content {
      position: absolute;
      width: 500px;
      margin-left: auto;
      margin-right: auto;
    }
  </style>
</head>

<body>
  <div class='background'>
    <div class='confirmation-item'>
      <div class='confirmation-content'>
        <img src="{{data.image}}" class='image' />
        <p style="font-size: 1.5rem;">Thank you for using Subsocial.</p>
        <p>{{data.message}}</p>
        <p>This link will expire in 1 hour</p>
        <button>
          <a href="{{data.link}}" class='buttonLink'>{{data.buttonText}}</a>
        </button>
      </div>

    </div>
  </div>
</body>
<html>
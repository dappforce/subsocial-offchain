<!DOCTYPE html>
<html>

<head>
  <title>Notifications</title>
  <meta charset="utf-8">
  <style type="text/css">

    body, td, th, a, div {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol', 'Noto Color Emoji';
    }

    .buttonLink {
      color: #fff !important;
      background: #eb2f96;
      border-color: #eb2f96;
      font-weight: 400;
      white-space: nowrap;
      text-align: center;
      border: 1px solid transparent;
      touch-action: manipulation;
      height: 32px;
      padding: 4px 15px;
      font-size: 14px;
      border-radius: 2px;
      margin-bottom: 10px;
      cursor: pointer;
      text-decoration: none;
    }

    td, th {
      padding: .5rem;
      line-height: 1.5rem;
    }

    td {
      font-size: 1rem;
    }

    th {
      font-size: 1.5rem;
    }

    table {
      padding-bottom: 1rem;
      padding-top: 1rem;
      text-align: center;
      width: 500px;
      margin-left: auto;
      margin-right: auto;
    }

    .image {
      margin-bottom: 1rem;
    }
    
  </style>
</head>

<body>
  <table cellspacing="1">
    <tr>
      <th>
        <img src="{{data.image}}" width="80" height="80" class='image' />
        <div>Thank you for using Subsocial</div>
      </th>
    </tr>
    <tr>
      <td>
        <div>{{data.message}}</div>
      </td>
    </tr>
    <tr>
      <td>
        <a href="{{data.link}}" class='buttonLink'>{{data.buttonText}}</a>
      </td>
    </tr>
    <tr>
      <td>
        <div>Or follow this link: {{data.link}}, link will expire in 1 hour</div>
      </td>
    </tr>
   </table>
</body>
<html>